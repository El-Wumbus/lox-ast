// Generates the first 21 fibonacci numbers

var a = 0;
var temp;

for (var b = 1; a < 10000; b = temp + b)
{
    print a;
    temp = a;
    a = b;

    if (a > 1000)
    {
        break;
    }
}

print "We broke?";
break;
print "I broke the wrong thing";